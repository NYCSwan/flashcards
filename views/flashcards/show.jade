extends ../layout

block content
    h1.
         #{title}
    ul
        - each flashcard, i in flashcards
            li
              =flashcard.question
              =flashcard.answer
              =flashcard.correct
             form(action='/blobs/#{blob._id}/edit',method='post',enctype='application/x-www-form-urlencoded')
                    input(type='hidden',value='DELETE',name='_method')
                    button(type='submit').
                      Delete


